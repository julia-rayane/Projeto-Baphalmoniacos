import { Router } from 'express';
// Importaremos os controllers aqui na próxima etapa!

const router = Router();

// As definições de rotas vão entrar aqui assim que migrarmos os controllers
router.get('/'); 

export default router;
